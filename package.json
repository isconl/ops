{
  "name": "@isconl/ops",
  "version": "0.1.0",
  "private": false,
  "description": "isconl-agent engine: live control surface for every fleet service and the OCI VM (status, VM stats, log tail, restart/start/stop/destroy, deploy status)",
  "main": "src/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "@bitwarden/sdk-napi": "^1.0.0"
  },
  "license": "MIT"
}
